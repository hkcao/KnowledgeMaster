# 知屿 KnowledgeMaster

> 面向科研论文学习的本地优先 macOS 知识工作台。

知屿希望把“收集论文 → 精读与批注 → 整理研究笔记 → 跨论文问答 → 沉淀个人理解”放进同一个桌面应用。它适合需要长期阅读论文、梳理研究主题、比较多篇文献并借助 AI 辅助思考的学生、研究人员和工程师。

项目目前处于早期开发阶段，仅提供 macOS 原生版本。

## 主要功能

### 1. 面向论文的资料管理

- 导入 PDF、Word（DOC/DOCX）、HTML、Markdown、TXT，也可以输入网址保存网页 HTML、递归导入目录或直接从 Finder 拖入。
- 原始资料统一保存在可配置的 `source/` 目录；选择 iCloud Drive 路径后，可依赖系统能力同步资料库。
- 以虚拟主题组织论文：一篇文档可同时属于多个主题，不需要复制原文件。
- 支持主题及子主题创建、改名、删除，以及通过拖拽调整文档归属；变更后可立即参与检索。
- 按文件名、主题和正文关键词检索，不依赖向量数据库，适合个人规模的数千份资料。
- 导入时按文件名默认去重，并通过 SHA-256 识别同内容副本。
- 尝试为论文生成“一作 et al, 论文标题”形式的展示名，并推荐可能的主题；最终分类由用户确认。

### 2. 专注的论文阅读体验

- 使用 PDFKit 阅读 PDF，并直接预览 Word、HTML、Markdown 和纯文本。
- PDF 每页右上角支持设置一个持久化阅读书签，再次打开文档时会自动回到书签页。
- 多标签页并行打开文档，适合在相关论文之间来回比较。
- 可展开文档目录导航，与正文同时显示，快速跳转长论文的章节或页面。
- 支持鼠标划词、Ask AI、引用，以及“原文 + 选区截图”的多模态上下文。
- 支持高亮、下划线和批注；批注入口贴近对应正文位置，可跳转和再次编辑。
- 可导出原始文档，也可将 PDF 高亮、下划线和批注内容合并后导出。

### 3. 研究笔记与知识沉淀

- 注释类笔记与论文中的具体位置关联，可从笔记列表跳回原文。
- 总结类笔记以 Markdown 文档保存，支持编辑态与渲染态。
- Markdown 渲染支持常用 LaTeX 行内公式和块级公式。
- 总结笔记可以关联多条注释笔记，用于形成文献综述、方法比较和阶段性思考。
- 保留对话历史，支持为单次对话生成并渲染摘要，也支持按主题累计摘要。

### 4. AI 辅助阅读与跨文献问答

- 支持 DeepSeek、智谱 GLM 和自定义 OpenAI 兼容接口。
- 支持 Claude Code、Codex 等本地 Agent，通过工具按需读取资料、检索文件和处理任务。
- 可选择当前页面、当前文档、某个主题或多个文档作为问答范围。
- 未选择文本或资料范围时，不默认加载文档，保持纯聊天并让 Agent 按需探索。
- API 模式可选择：
  - **相关片段模式**：从已提取文本中检索关键词相关片段，减少无关上下文。
  - **自主工具模式**：由模型按需列目录、搜索和读取文件。
- Agent 模式优先使用只读的原始资料副本，不依赖预先切分的文本；若已有 PDF 解析或 OCR 衍生文件，会一并提示 Agent 复用。
- Agent 执行过程可折叠查看、实时输出并手动终止，不使用固定的 3 分钟任务超时。
- Agent 调研产生的下载文件会先进入待确认区，由用户确认文件名和虚拟主题后再导入资料库。
- 聊天消息、对话摘要和研究笔记均按 Markdown 渲染，并支持常用 LaTeX 公式。

### 5. 本地优先与资料安全

- 资料、主题、笔记、批注和对话历史默认保存在用户选择的本地资料库中。
- API Key 保存在 macOS 钥匙串；应用启动和普通设置浏览不会读取，仅在实际调用、测试连接或修改凭据时按需访问并在进程内缓存。
- Agent 工作区使用只读资料副本，避免工具误删或覆盖原始文件。
- 模型配置与钥匙串凭据不写入可同步的资料库目录。
- 元数据采用原子写入，并保留上一版本备份。

## 一个典型的科研工作流

1. 将新下载的论文拖入知屿，检查展示名和推荐主题。
2. 在多标签页中对照阅读相关论文，通过目录导航定位方法、实验和结论。
3. 对关键段落高亮、划线并记录批注；需要时直接基于选区向 AI 追问。
4. 选择一个主题或多篇论文，让模型比较研究假设、方法、数据集与局限。
5. 将重要批注关联到 Markdown 总结笔记，逐步形成综述或自己的研究判断。

## 当前遗留功能

1. **对话记忆暂时缺失**：当前可以保存历史与摘要，但尚未实现长期、可自动召回的跨会话记忆；模型不会主动从过去的对话中检索并注入相关信息。
2. **模型交互上下文的 Token 性价比尚未优化**：已有相关片段和自主工具两种路径，但上下文压缩、缓存复用、预算控制及多轮增量传递仍需要系统优化。
3. **Agent 对 PDF 等格式解析的交互尚未优化**：复杂版式、扫描件、表格、公式和 OCR 结果的生成、复用与质量判断还不够成熟，可能产生重复解析和较长等待。
4. **界面美观性仍需提升**：当前优先验证完整工作流，视觉层级、间距、动效、空状态及一致性仍有较大改进空间。
5. **暂未支持 Windows**：当前仅支持 macOS 14 及以上版本，尚无 Windows 客户端。

## 技术与运行环境

- 原生 SwiftUI + AppKit
- PDFKit / WebKit / Security.framework
- Swift 5.10
- macOS 14+
- 无 Electron、无向量数据库

开发运行：

```bash
swift run
```

运行测试：

```bash
swift test
```

构建可双击启动的应用：

```bash
./scripts/build-app.sh
open release/KnowledgeMaster.app
```

当前构建产物未进行 Developer ID 签名、公证或 DMG 封装。面向其他用户正式分发前仍需完成这些步骤。

## AI 配置

首次启动不会要求访问钥匙串。需要使用 AI 时，在设置中选择接入方式：

- **API 模式**：填写服务商、Base URL、模型名和 API Key。
- **Claude Code**：安装并登录 Claude Code CLI，然后在设置中选择对应模式。
- **Codex**：安装并登录 Codex CLI，然后在设置中选择对应模式。

可在设置中执行连接测试。对 API Key 的钥匙串授权只会在真正需要读取或修改凭据时触发；macOS 是否再次显示系统授权框，还取决于钥匙串访问控制和应用签名状态。

## 本地资料结构

```text
<资料库目录>/
├── knowledge.json
└── source/
    ├── documents/
    │   └── <扁平保存的导入原始文件>
    ├── downloads/
    │   └── pending/
    ├── generated/
    ├── index/
    └── notes/
```

- `source/` 是底层文件区，界面主要通过主题、检索和笔记等虚拟视图访问资料。
- 导入文件扁平保存在 `source/documents/`，不为每份文档额外创建文件夹。
- `downloads/pending/` 保存 Agent 下载后等待用户确认的文件。
- `generated/` 保存 Agent 产生的解析结果、OCR、摘要或其他衍生文件。
- `index/` 保存可重建的全文索引或缓存。
- `notes/` 保存总结类 Markdown 笔记。
- `knowledge.json` 保存虚拟主题、关联关系、对话、摘要、批注和笔记元数据。

将资料库设置到 iCloud Drive 后，资料与元数据由 macOS 同步。为避免冲突，请不要在多台 Mac 上同时修改同一个资料库。

## 当前实现边界

- 关键词搜索以本地线性检索为主，适合个人资料库；后续可以替换为 SQLite FTS。
- API 的相关片段模式使用本地提取文本；Agent 模式读取原始文件，并可复用已有衍生解析文件。
- 扫描版 PDF 没有内置固定 OCR 流水线，当前交由 Agent 或外部工具按需处理。
- Agent 的联网能力取决于所使用的 CLI、登录状态、沙箱与网络权限，知屿本身不会绕过这些限制。
- 删除文档会影响 `source/` 中的原始文件，界面应明确确认；删除主题只解除虚拟关联，不删除原文。

欢迎通过 Issue 反馈论文阅读、批注、研究笔记和 AI 辅助研究工作流中的实际问题。

## 许可证

本项目采用 [MIT License](LICENSE)。
